# 1.0.0-alpha.2

## 更新

* **命令**
  + 更新分类 `dir`，用于文件夹操作
  + 更新命令 `file.exists`，用于检测文件是否存在
  + 更新命令 `dir.create`，用于创建文件夹
  + 更新命令 `dir.delete`，用于删除文件夹
  + 更新命令 `dir.exists`，用于检测文件夹是否存在

## 优化

* **错误报告**: 在 `control.if` 或 `control.loop` 中的子命令发生错误时，其位置报告不再为if或loop父命令，而是更加精确的子命令

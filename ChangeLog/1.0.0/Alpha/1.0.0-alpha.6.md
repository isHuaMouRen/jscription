# 1.0.0-alpha.6

## 更新

* **命令**
  + 新增分类 `path` 用于路径操作
  + 新增命令 `path.combine` 用于安全的路径拼接
  + 新增命令 `path.getfilename` 用于获得文件名
  + 新增命令 `path.getextension` 用于获得文件扩展名
  + 新增命令 `string.substring` 用于获得指定字符串中的某一段字符串
